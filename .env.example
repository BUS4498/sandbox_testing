# TEMPLATE ONLY — NO REAL CREDENTIALS
# Copy to a local .env file only after an implementation exists.
# Never commit the populated .env file.
#
# These provider-neutral names are provisional. The later implementation may
# adapt them to the selected email approach.

OPENAI_API_KEY=
NOTIFICATION_EMAIL=
EMAIL_SERVICE_USERNAME=
EMAIL_SERVICE_PASSWORD=
